// A route file is required for the tab to register, but this screen is never
// shown: tabPress is intercepted in _layout.tsx and opens a sheet instead.
export default function CreateTabPlaceholder() {
  return null;
}
